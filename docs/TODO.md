# Branding work still requiring approval

- Redraw the organization avatar and current raster artwork as native vectors.
- Visually compare every native-vector proposal with its approved PNG reference.
- Create a true single-color organization-avatar silhouette for `monochrome.svg`.
- Confirm the final production Health Hub URL used by launcher packaging.
- Decide whether daemon-specific favicons should be deployed or whether all tabs use the organization avatar.
- Add checksums and release versions when the branding repository is published.
